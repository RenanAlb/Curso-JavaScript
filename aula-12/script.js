function parimpar(n) {
    if (n % 2 == 0) {
        return 'Par!';
    } else {
        return 'Impar!'; // retornos
    }
}

console.log(parimpar(46));
// O número entre parênteses no parimpar será jogado na variável (n);